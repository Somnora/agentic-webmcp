# Public shop hostname only. Optional live catalog uses the Storefront API.
CATALOG_SHOP=agentic-app-review-test.myshopify.com

# Optional read-only Storefront API token. Set in Wrangler secrets or .dev.vars.
# Never commit this value. Admin API tokens are not used and must not be added.
# CATALOG_STOREFRONT_TOKEN=
